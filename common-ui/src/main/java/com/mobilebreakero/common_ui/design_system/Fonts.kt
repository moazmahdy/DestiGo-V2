package com.mobilebreakero.common_ui.design_system

import androidx.compose.ui.text.font.Font
import androidx.compose.ui.text.font.FontFamily
import androidx.compose.ui.text.font.FontWeight
import com.mobilebreakero.common_ui.R

object Fonts {
    val Roboto = FontFamily(
        Font(resId = R.font.robot, weight = FontWeight(400)),
    )
    val ArialRegular = FontFamily(
        Font(resId = R.font.araa, weight = FontWeight(600)),
    )
}